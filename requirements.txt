# PDF 文本/Markdown 提取工具依赖
# 安装：python -m pip install -r requirements.txt

# 核心提取库（脚本会自动安装，此处仅供手动安装参考）
pypdf>=3.0.0           # 纯 Python，最稳定，优先使用
pdfplumber>=0.10.0     # 支持表格提取，md 格式首选
pdfminer.six>=20221105 # 精确布局分析（后备）

# pdfplumber 依赖链修补（Linux PEP 668 / Windows 缺失场景）
cffi>=1.15.0           # cryptography → _cffi_backend
cryptography>=41.0.0   # pdfminer 解密需要

# 可选（OCR 扫描件）
# pdf2image>=1.16.0
# pillow>=9.0.0
# pytesseract>=0.3.10